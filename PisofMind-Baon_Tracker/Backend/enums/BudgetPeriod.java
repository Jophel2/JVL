package com.org.PisofMind.enums;

/**
 * Enum representing the budget time period for category budgets.
 * Used to keep budget inputs explicit and consistent for the user.
 */
public enum BudgetPeriod {
    DAILY,
    WEEKLY,
    MONTHLY,
    YEARLY
}
